import { React } from 'react';
import LoginForm from '@/components/LoginForm';

const Login = async () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Login;
